# Sensitive — contains API tokens, MCP credentials
.env.keys
settings.local.json
.credentials.json

# MCP server (see workflow-upgrade/setup/jira-mcp-setup.md for setup instructions)
mcp-jira-server/

# Claude Code internal / ephemeral
# memory/ and sessions/ are force-tracked via `git add -f` for portability
# New project memory/sessions must be added with: git add -f projects/{key}/memory/ projects/{key}/sessions/
projects/
todos/
statsig/
credentials/
cache/
history.jsonl
stats-cache.json
telemetry/
debug/
ide/
sessions/
tasks/
plugins/
file-history/
shell-snapshots/
paste-cache/
backups/
plans/
dashboard/

# Logs
logs/

# Personal notes (not part of claude config)
habit-plan.md

# Archives
*.tar.gz
*.zip

# Context compact tracking (generated by PreCompact hook)
.compact-log
.compact-marker

# OS
Thumbs.db
.DS_Store
